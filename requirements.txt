# ============================================================
# REQUIREMENTS - Unidades Geradoras Backend
# ============================================================

# ------------------------------------------------------------
# Framework Principal
# ------------------------------------------------------------
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1

# ------------------------------------------------------------
# Banco de Dados - SQLAlchemy + PostgreSQL
# ------------------------------------------------------------
sqlalchemy==2.0.30
alembic==1.13.1
asyncpg==0.29.0          # Driver async para PostgreSQL
psycopg2-binary==2.9.9   # Driver sync para PostgreSQL (Alembic)

# ------------------------------------------------------------
# Autenticação e Segurança
# ------------------------------------------------------------
python-jose[cryptography]==3.3.0   # JWT Token
passlib[bcrypt]==1.7.4             # Hash de senhas
bcrypt==4.1.3
python-multipart==0.0.9            # Upload de arquivos / form-data

# ------------------------------------------------------------
# Celery + Redis (Tarefas Assíncronas)
# ------------------------------------------------------------
celery==5.4.0
redis==5.0.4
flower==2.0.1                      # Monitoramento do Celery

# ------------------------------------------------------------
# Cache e Sessão
# ------------------------------------------------------------
aioredis==2.0.1

# ------------------------------------------------------------
# HTTP Client Async
# ------------------------------------------------------------
httpx==0.27.0
aiohttp==3.9.5

# ------------------------------------------------------------
# Processamento de Dados (Unidades Geradoras)
# ------------------------------------------------------------
pandas==2.2.2
numpy==1.26.4
openpyxl==3.1.2          # Leitura/escrita de arquivos Excel
xlrd==2.0.1              # Leitura de arquivos .xls antigos

# ------------------------------------------------------------
# Serialização e Parsing
# ------------------------------------------------------------
python-dateutil==2.9.0
orjson==3.10.3            # JSON ultrarrápido para FastAPI

# ------------------------------------------------------------
# Logging
# ------------------------------------------------------------
structlog==24.2.0         # Logs estruturados em JSON com contexto
rich==13.7.1              # Output colorido no terminal (dev)

# ------------------------------------------------------------
# Variáveis de Ambiente
# ------------------------------------------------------------
python-dotenv==1.0.1

# ------------------------------------------------------------
# Validação e Utilitários
# ------------------------------------------------------------
email-validator==2.1.1
pytz==2024.1
uuid==1.30

# ------------------------------------------------------------
# Testes
# ------------------------------------------------------------
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0
httpx==0.27.0              # Usado também no TestClient do FastAPI
factory-boy==3.3.0         # Geração de dados fake para testes
faker==25.0.0

# ------------------------------------------------------------
# Qualidade de Código
# ------------------------------------------------------------
ruff==0.4.4                # Linter + Formatter
mypy==1.10.0               # Type checker
pre-commit==3.7.1
